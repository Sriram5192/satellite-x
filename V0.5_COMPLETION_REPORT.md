# SATELLITE-X v0.5.0 — Pending-Scope Completion Report

**Date:** 2026-08-17  
**Policy:** no fabricated production data, no credential bypasses, no unsupported production claims.

## Completed software scope

### Safe Sentinel-1 integration

- Planetary Computer Sentinel-1 RTC VV/VH clipping is live.
- Real latest scene: `S1D_IW_GRDH_1SDV_20260817T003010_20260817T003035_004158_0079BF_rtc`.
- 96 valid dual-polarization pixels.
- VV mean `-13.138436 dB`; VH mean `-21.678322 dB`; VV−VH `8.539886 dB`.
- Independent direct-COG oracle: **5/5 PASS**.
- Optical/SAR resilience routing is now explicit:
  - accepted optical + SAR → `OPTICAL_PRIMARY_SAR_ANCILLARY`;
  - missing optical + accepted SAR → `SAR_ONLY_UNRESOLVED`;
  - SAR-only routing disables optical indices and diagnosis;
  - SAR cannot override location/urban rejection.

### Multi-scene crop workflow

- Three real Sentinel-2 Set 3 observations are processed as a crop-aware series.
- All seven index trends are included.
- Independent ordinary-least-squares oracle: **16/16 PASS**.
- Output remains honestly `limited` because the maximum cloud-driven scene gap is 33 days.

### Authenticated individual workflow

- Salted PBKDF2 password hashes; plaintext passwords are never stored.
- Random sessions are stored only as SHA-256 token hashes and can be revoked.
- Confirmed FTW/user-drawn/official-FMB field links are explicit.
- `require_owned_field` prevents a farmer from accessing another field.
- Administrator CLI supports account registration through an environment-held initial password and confirmed-field linking.

### Provider OTP

- Generic authorized JSON SMS transport is implemented.
- E.164 validation, HMAC-hashed phone identifier and OTP, five-minute expiry, request rate limit, attempt limit, constant-time comparison and single-use consumption are implemented.
- Successful OTP proof creates a revocable identity session; proof IDs cannot be consumed twice.
- Real SMS sending remains disabled until a provider endpoint and credentials are supplied.

### Government authorization and official connector

- Village, mandal and district permissions/jurisdictions are separate.
- Aggregate access remains aggregate-only; identifiable parcel evidence still requires a case ID.
- Official JSON connector enforces policy before network access, HTTPS for non-local endpoints, environment-held bearer credentials, authorization ID and purpose headers, and JSON-only responses.
- Disabled or missing-credential connectors return `activation_required` rather than scraping or bypassing CAPTCHA/OTP.
- Local authorized HTTP adapter execution passed; the placeholder government example correctly returns `CONNECTOR_DISABLED` without making a request.

### Offline mobile workflow and secure synchronization

- Installable PWA under `mobile/`.
- Service-worker app shell, IndexedDB queue, camera input, high-accuracy geolocation, SHA-256 photo hashing and offline status are implemented.
- Session token is kept only in page memory.
- FastAPI endpoints provide health, OTP request/verification, current principal and evidence batch sync.
- Server validates authenticated officer identity, evidence hash, pending state and event-ID collisions.
- Replays of identical events return the same receipt; conflicting payloads are rejected.
- Cross-language JavaScript/Python canonical digest audit: **5/5 PASS**.
- HTTP PWA/OTP/sync flow audit: **9/9 PASS**.
- Uvicorn PWA/API startup, `/api/health`, root HTML and explicit unconfigured-OTP response were executed successfully.

### Yield interface

- Verified-label contract accepts only CCE, weighbridge or audited-procurement evidence references.
- Minimum 100 records and two independent seasons.
- Duplicate field/season records and inconsistent feature sets are rejected.
- Ridge fitting uses leave-one-season-out validation to reduce temporal leakage.
- Dataset fingerprint, R², MAE and RMSE are emitted.
- Deterministic validation fixture achieved R² `0.999478` but is permanently tagged `validation_fixture_only` and cannot be approved.
- A production candidate requires `dataset_purpose=production_verified`, a matching candidate hash and named human approval.
- Even approved estimates remain experimental and `production_authorized=false`.

### VRA and machinery interface

- Live within-field low/medium/high relative-NDVI zones are generated without automatic rates.
- Agronomist approval is required before rates can be attached.
- Equipment profile checks rate unit and min/max limits.
- Operator approval requires calibration, boundary and emergency-stop checks.
- Output is a hashed GeoJSON transfer package; `automatic_execution_authorized=false` always.

## Validation summary

```text
74/74 deterministic tests PASS
6/6 live tests PASS
26/26 JSON schemas structurally valid
12/12 Set 3/4 independent checks PASS
5/5 Sentinel-1 direct-COG checks PASS
16/16 multi-scene OLS checks PASS
5/5 PWA/Python digest checks PASS
9/9 authenticated mobile API-flow checks PASS
Streamlit recovery → SAR → multimodal route → Set 2 → Set 3/4 PASS
```

## External activation inputs still required

The corresponding software paths are implemented and tested, but these real-world inputs cannot be manufactured by the system:

1. official government API/MoU, endpoint and credentials;
2. SMS provider endpoint, sender approval and credentials;
3. verified local multi-season yield labels and signed human validation;
4. target machinery profile, agronomist rates, operator approval and physical field trial;
5. target-device GPS/camera test, production TLS/secrets deployment and field acceptance;
6. local SAR ground labels before crop-stress thresholds can be scientifically calibrated.

Until those inputs are supplied, the system returns explicit `activation_required`, `validation_fixture_only`, `SAR_ONLY_UNRESOLVED`, or non-automatic states. It never fabricates activation success.
